package com.a6.ensayomokito.login;

import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.junit.MockitoJUnitRunner;

import static org.mockito.Mockito.verify;

/*
@RunWith(MockitoJUnitRunner::class)
public class TestJava {

    @Mock
    private Login.Presenter presenter;

    private LoginModel model;

    @Before
    public void setUp() throws Exception{
        model = new LoginModel(presenter);
    }

    @Test
    public void shouldSuccerWithValidaUserPass() throws Exception {
        model.validaUser("ale", "123");

        // Verifica que se llama un metodo
        verify(presenter).usuarioValido();
    }

    @Test
    public void shoulcNotSuccessWithThisUserandPass() throws  Exception {
        model.validaUser("ale", "234");
        verify(presenter).error();
    }

}

 */
